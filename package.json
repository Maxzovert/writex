{
  "name": "your-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd writex && npm run dev",
    "client": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
